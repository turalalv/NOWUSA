import type {LoaderFunctionArgs} from 'react-router';
import db from '../db.server';
import {finishGoogle} from '../lib/google.server.mjs';
export const loader=async({request}:LoaderFunctionArgs)=>{const p=new URL(request.url).searchParams;try{await finishGoogle(db,{state:p.get('state'),code:p.get('code'),error:p.get('error'),cookie:request.headers.get('cookie')});return new Response('Google bağlantısı hazırdır. Bu pəncərəni bağlayıb Shopify tətbiqində Google bölməsini yenilə.',{headers:{'Content-Type':'text/plain; charset=utf-8','Cache-Control':'no-store','Referrer-Policy':'no-referrer','Set-Cookie':'nosweat_google_state=; Path=/google; HttpOnly; Secure; SameSite=Lax; Max-Age=0'}});}catch(error){return new Response(error instanceof Error?error.message:'Google bağlantısı alınmadı.',{status:400,headers:{'Content-Type':'text/plain; charset=utf-8','Cache-Control':'no-store','Referrer-Policy':'no-referrer'}});}};
